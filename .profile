#!/bin/sh

#
# ~/.profile
#
# Contains items not specifically related to bash, such as environment
# variables like PATH.
#

export NVM_DIR=${HOME}/.nvm
export PATH=/usr/local/bin:/usr/local/sbin:$PATH
export WORKON_HOME=${HOME}/virtualenvs
